// Copie este arquivo para config.js (gitignored) e ajuste os valores pro
// ambiente onde este painel está sendo servido. Sem config.js, o app cai
// nos defaults de desenvolvimento (localhost).
window.DAQUI_CONFIG = {
  API_URL: 'http://localhost:8001/api/v1',
  APP_URL: 'http://localhost:8081',
};
